# JARVIS bounded restart

The September 11, 2026 restart is a source-only consolidation owned by one
integrator. Budget: 90 minutes from 02:48:56 UTC, with a checkpoint at 03:18:56
UTC. Two checkpoints without a commit, test result or durable artifact stop the
attempt. No merge, deployment, provider operation or production activation is
part of this work.

## Immutable starting points

| Source | Exact commit | Disposition |
| --- | --- | --- |
| Observed main | `5030afc162afded656e783639ae5f24763eb6309` | Preserve the merged foundation; no merge performed |
| #495 merge base | `3b16e406a0c303e8c859d5eaadca8fab27d0adac` | Already includes all PRs #484–491 |
| PR #495 | `125af011d11cc176f89ad685eafc6d203ed0ce98` | Preserve on `codex/ovd-505-preserved-125af011`; narrow the existing draft by a forward commit |
| PR #492 | `0b3ba54ff08c12b9c489d2b2e162cad769d1d48e` | Leave unchanged; plan journal integration after the evidence slice |

PRs #484–491 already supply durable intake, ordered changes, pairing/sessions,
gateway, companion state, cumulative edits, exact STEP export and native
ownership. Reuse these primitives; do not cherry-pick them again. PR #492 changes
the native runner and journal, while the narrowed #495 checks stored evidence.
Their only currently overlapping changed file is `TEST_STRATEGY.md`; reconcile
its descriptions once when the source is eventually integrated. Source merge
does not establish a qualified connected runtime.

## Source split

1. **Keep in #495:** bounded byte/report validators, fixed synthetic fixtures,
   exact preview validation, negative tests and a reproducible 7 mm evidence
   replay. No HTTP entrypoint or database mutation. Keep the existing seven native
   checks and fixed two-cylinder qualification policy rather than inventing a
   generalized CAD framework.
2. **Defer persistence and verifier authority:** the four unpublished migrations,
   verifier client and PostgreSQL integration runners are preserved at the
   starting #495 commit and removed from the active diff. They must be re-admitted
   as a bounded follow-up after a least-privilege design. Revoking a direct role
   grant does not remove inherited `PUBLIC` schema usage or `PUBLIC EXECUTE`.
   Do not copy the preserved migrations wholesale into a deployable branch.
3. **Defer recovery and preview association:** immutable failure history, owner
   retry and quarantine are later database work, not prerequisites for proving
   one retained preview. Preserve their tests with their source, and require
   real database/race evidence when those slices return.

The deferred work is routed to OVD-509 (trusted Windows filesystem admission),
OVD-510 (effective verifier privileges) and OVD-511 (atomic finalization), all in
Backlog. Existing OVD-507 retains later review-artifact publication. OVD-505
remains the parent outcome; a completed evidence slice cannot close the original
durable-finalization acceptance criteria. These are implementation boundaries
for already approved work, not permission to activate the loop.

Acceptance for the kept slice: malformed or aliased trusted filesystem identity
fails before object reads; report paths bind to admitted roots; altered bytes,
missing predicates, wrong snapshots and contradictory measurements fail; derived
text fixtures use synthetic paths and truthful hashes/provenance; the CLI replay
produces one exactly bound 7 mm STEP preview without claiming new CAD execution.

## Smallest connected demonstration still required

Target one editable synthetic SolidWorks part, `parts/baseline-5mm.SLDPRT`, in
the already-qualified assembly. Its fixed companion remains unchanged. Interpret
the one supported action, **set depth to 7 mm**, then execute once against one
exact admitted predecessor, verify the result and display its exact exported
geometry. Do not add a new standalone-part topology or broader command grammar.

The checked-in native candidate bytes currently prove a retained 5 → 8 mm run;
the preview fixtures prove retained 9 mm and 7 mm exports. Do not splice those
independent runs together as a new 5 → 7 mm native result. The evidence replay
can validate the retained 7 mm snapshot and preview but cannot bridge this gap.

Before a connected demo, a trusted Windows admission boundary must capture
filesystem identity from open handles for the input and candidate directories,
including junction/symlink/substituted-drive resolution. Bind volume and file
identity to the same attempt, hold or revalidate those identities across native
work, reject aliases/identity changes and retain the observations. Test injected
identities are explicitly simulations. No report string or path normalization
can create that authority.

PR #492 integration must first establish exact-head Windows Desktop 5.1 readiness
and preview-helper qualification for its latest extraction. Preserve older native
receipts at their original source hashes; do not relabel them as current-head
qualification. Then connect only the fixed command, immutable artifact handoff,
trusted admission and verifier result. UI, transport and admission must each
report their actual state; journal exit observations alone never approve a
candidate or permit retry.

## Complexity and verification

The original 49-file, 5,354-added-line PR is High complexity. The user explicitly
requested this split. The remaining pure evidence slice is Medium: it retains
bounded CAD validation and fixtures but adds no schema, runtime dependency,
execution authority or UI behavior. Recommendation: proceed with caution and
exercise every changed admission boundary.

Run targeted report/result/preview and existing cumulative/viewer tests first,
then repository verification. Record failures, timeouts and unreached checks.
Hosted quality acceptance, source review, Windows qualification and a connected
demo are separate gates. No historical green run substitutes for the current
head. The prior task reported local proof commit
`eba2f99446fff5b4c5967e30a756b60998b6ad7e`; that object is unavailable in this
checkout and is not represented as integrated evidence.

Rollback of this source slice is a revert of its forward commits. No production
state exists to migrate or reverse. Preserve the original source and retained
evidence; do not clear attempts, fences or native occupancy.
