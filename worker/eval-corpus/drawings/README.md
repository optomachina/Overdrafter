# Corpus drawings

Scrubbed drawings live here. Run every file through
`scripts/scrub_drawing.py` before adding it — see `../README.md`.

No drawing is currently checked in. This repository is public, so scrubbing
alone is not permission to publish a customer-derived drawing. Add a case only
when its exact bytes are approved for public distribution. Never use a private
validation package as application fixture or corpus data.
